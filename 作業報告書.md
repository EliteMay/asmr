# ASMRTube 作業報告書


## 2026-09-19 v3.0.2 日常利用の欠点修正

### 目的

現行v3を大きく作り直さず、普段使いで詰まりやすい操作と明確なBugを先に修正した。

他Projectの参照では、LyricTubeのプレイリスト管理から「番号入力ではなく、その場で所属を切り替えられる管理UI」という構造だけを再利用した。v2.4で失敗したため、LyricTubeのVisual自体はコピーしていない。

### 修正した欠点

- 作品音量へ `0` を入力すると `35` として保存されるBugを修正
- プレイリスト追加時のBrowser `prompt` / 番号入力を廃止
- プレイリスト管理ダイアログで追加 / 解除 / 新規作成 / 削除をまとめて操作可能に変更
- MobileでTopbar actionsが全非表示になり、タグ絞り込みへ到達できなかった導線を修正
- 作品情報カードへ「お気に入り」「編集」を追加し、Mobileでも主要操作を失わないようにした
- Sound Mapのタブ / メタ情報 / 見出しカードで7〜9pxだった文字を読みやすいサイズへ引き上げた

### Storage / compatibility

- `asmrtube.library.v1` を維持
- Playlist schemaは既存 `{id,name,items[]}` のまま
- Library Schema Versionは `1` のまま
- Theme / Settings keyは変更なし
- YouTube Runtime / Timestamp Parserの責務変更なし

### Regression Guard

Browser Smokeへ次を追加。

- 作品音量 `0` の保存
- 作品情報カードのFavorite / Edit操作存在
- Playlist managerの表示とmembership保存
- 560px幅でもタグ絞り込みButtonが非表示にならないこと

Static Checkへ次を追加。

- `prompt` 型Playlist UIの再混入防止
- Playlist manager DOMの存在
- Volume `0` を潰す `|| 35` の再混入防止
- Mobile Filter導線の存在

### Version / Guide

- App Version: `3.0.2`
- Build: `20260919-1`
- Adopted Guide baseline: `1.22.0`
- Current Guide mainの未Release変更も今回必要範囲で確認

### Validation state

- Branch implementation: complete
- PR CI: pending
- GitHub Pages final deployment: not yet verified
- Mobile real-device: not verified


## 2026-09-01 v3.0.0 基礎再設計 / ASMR Media Deck

### 背景

v2.4のVisual RefreshをGitHub Pagesへ反映後、ユーザーから次の評価を受けました。

- 「シンプルになっただけで微妙」
- 全体評価: **40 / 100**
- 「一度いちから考え直して基礎的なところからやり直す」
- 独立した設定ページと複数の色Themeを追加したい

このFeedbackを受け、v2.4をVisual Baselineとして採用せず、`web-project-guide` v1.11.0のDesign Direction手順からやり直しました。

### Design Direction比較

実装前に3案を比較しました。

1. **LyricTube型3 Paneをさらに磨く**
   - 操作性は高い
   - v2.4の延長に見えやすく、今回の問題を解決しにくいため不採用
2. **Theater型: 動画を上、情報を下**
   - Videoは強く見せられる
   - Timestampへ頻繁に移動する主要Taskで上下移動が増えるため不採用
3. **ASMR Media Deck**
   - Library / Player / Timestampの実用Hierarchyは維持
   - Player周辺だけを大胆にし、選択中ArtworkをVisual materialとして利用
   - Timestampを `Sound Map` として再定義
   - 採用

### v3 Signature

```text
Library rail
→ ASMR Media Deck
→ Sound Map / Timestamps
```

選択中ASMRのYouTubeサムネイルをPlayer周辺へ弱くBlur表示し、現在聴いている作品そのものを画面のIdentityに使います。

GenericなGradientや大量のDecorative Cardで埋めるのではなく、Media contentを主役にします。

### 設定ページ

旧設定Modalを主要導線にせず、Sidebarから開く独立設定Pageを追加しました。

Theme:

- Moon Violet
- Soft Rose
- Deep Ocean
- Quiet Forest
- Warm Lamp
- Graphite

Theme変更はAccentだけでなく次へ連動します。

- Page background
- Sidebar
- Topbar
- Surface
- Selected / Active state
- Player control
- Timestamp current state
- Settings page

その他:

- Brightness 30〜100%
- Compact display
- Thumbnail visibility
- Reduced motion
- Start dashboard
- Data management入口
- Help入口

保存は既存 `asmrtube.settings.v1` を維持し、任意項目 `theme` を追加します。

### Visual architecture

新規:

- `theme.css` — Palette / theme tokenの正本
- `settings.css` — 設定Pageの正本
- `appearance.js` — Theme / brightness / display / ambient artwork

更新:

- `workspace.css` — v2.4のFlat workspaceからMedia Deckへ全面再設計
- `index.html` — Settings page / Media Deck / v3 Runtimeへ再構成

### Runtimeの基礎問題も修正

v2.4 mainを確認すると、Repositoryに存在する以下の正式Moduleが `index.html` から読み込まれていませんでした。

- `app-config.js`
- `ui-enhancements.js`
- `timestamp-ui.js`

そのため、作成済みのProduct ShellやTimestamp UIが実サイトで働かない状態になっていました。

v3ではRuntimeを明示的に接続します。

```text
app-config.js
app.js
timestamp-parser.js
timestamp-ui.js
ui-enhancements.js
app-quality-v21.js
timestamp-polish-v21.js
library-tools-v22.js
appearance.js
```

`timestamp-ui.js` も正式Global rendererへ接続し直しました。

```text
renderTimestamps
updateActiveTimestamp
showTimestampPreview
```

これにより `見出し / すべて`、Chapter、Parent、折りたたみ、現在位置強調をBase Appの実行経路から利用します。

### Version / metadata

- App Version: `3.0.0`
- Build: `20260901-1`
- Adopted Guide: `1.11.0`
- Schema Version: `1` のまま

### 保存互換性

Library / Timestamp Schemaは変更していません。

維持:

```text
asmrtube.library.v1
asmrtube.settings.v1
asmrtube.timestamp.view.v1
asmrtube.snapshot.v1
asmrtube.snapshot.beforeRestore.v1
```

既存:

```text
resumeAt
resumeDuration
resumeUpdatedAt
favoriteSections
```

も維持します。

### Regression Guard

`tests/static-check.mjs` をv3向けに強化しました。

確認対象:

- 全Local CSS / JS参照先の存在
- JSON parse
- Version / Guide / Schema整合
- `theme.css → workspace.css → settings.css` のVisual load order
- `timestamp-parser → timestamp-ui → ui-enhancements → ... → appearance` のRuntime load order
- Dedicated settings pageの存在
- 複数Themeの存在と重複なし
- `asmrtube.settings.v1` 維持
- `timestamp-ui.js` がGlobal rendererへ正式接続されていること

### Project memory

`PROJECT_LEARNINGS.md` へ `L-002 Media hierarchyとVisual austerityを混同しない` を追加しました。

v2.4の40/100評価をEvidenceとして残し、今後「別Projectで装飾を減らしたこと」をそのままASMRTubeの成功要因としてコピーしないようにします。

`docs/VISUAL_BASELINE.md` はv2.4 CandidateをRejectedへ変更し、v3 ASMR Media Deckを新Candidateにしました。

### 現在の確認状態

- Implementation: **In progress / branch**
- Storage / Schema change: **No**
- JavaScript syntax: **PR CIで確認予定**
- Static project integrity: **PR CIで確認予定**
- GitHub Pages deployment: **Merge後に確認予定**
- Browser / Screenshot Visual Review: **Not verified**
- Mobile real-device: **Not verified**

v3はVisual大変更のため、CI成功だけでは完成扱いしません。最終Pagesをユーザーが確認するまではVisual Candidateです。

---

## 2026-08-31 v2.4.0 LyricTube Media Workspace Visual Refresh

### 目的

ASMRTubeの見た目を、ユーザー評価済みのLyricTubeの現行Visual Baselineと `web-project-guide` の `VD-001 LyricTube Media Workspace` を参考に再整理しました。

機能を増やす作業ではなく、日常利用の主要Taskである次の流れを視覚的に強くする変更です。

```text
ASMRを探す
→ 再生する
→ タイムスタンプから目的の場面へ移動する
```

### 参考にしたLyricTubeの設計

- Desktopの `Library rail → Player → Lyrics` 構成
- Sidebarの高密度だが明確なHierarchy
- Player / Reading SurfaceをPrimary surfaceとして扱う
- 補助操作をすべてCard化せず、Section / Dividerで整理する
- Active状態を大きなAccent塗りではなく `surface + accent line` で示す
- Gradient / Glow / ShadowよりTypography / Spacing / Divider / Surface差を優先する

ASMRTubeではLyricsをそのままコピーせず、右側を **Timestamp reading surface** として置き換えています。

### 実装内容

新規 `workspace.css` を現在のVisual composition Source of Truthとして追加しました。

主な変更:

- 背景の大きなGradientを削除し、LyricTubeに近いFlat dark workspaceへ変更
- Desktop Sidebarを高密度なLibrary railへ整理
- Search / View / Playlist / Libraryの階層をDividerとTypographyで明確化
- ASMR一覧のActive表示を左Accent line中心へ変更
- TopbarのBlur / Shadowを外し、現在作品のContext barとして簡素化
- PlayerだけはMediaの主役として強いBorderと必要最小限のShadowを維持
- Transport / Range / Infoを独立Cardから連続Sectionへ変更
- Resume / Favorite sectionもNested Card感を弱めた
- Timestamp panelをLyricTube Lyrics同様のReading Surfaceへ変更
- Timestamp active rowをAccent line + Surface差で表示
- 見出し / すべてTab、Chapter / Parent cardの装飾を弱めた
- Dashboard系VisualのHero / Stat Card / Recent CardをFlatな構造へ寄せた
- Dialog / Creator / Timestamp Editorも同じSurface tokenへ統一
- MobileではPlayer → Timestampの優先順を維持して縦に再構成

### Version / Guide metadata

- App Version: `2.4.0`
- Build: `20260831-1`
- Adopted Guide: `1.11.0`
- Schema Version: `1` のまま

`app-config.js / project-meta.json / index.html` のVersionを揃えました。

### 保存・互換性への影響

変更なし。

維持している保存キー:

```text
asmrtube.library.v1
asmrtube.settings.v1
asmrtube.timestamp.view.v1
asmrtube.snapshot.v1
asmrtube.snapshot.beforeRestore.v1
```

今回の変更では次に触れていません。

- YouTube Player behavior
- Timestamp parser
- Timestamp保存Schema
- Resume / Favorite sections保存形式
- JSON backup / import形式

### Regression Guard

`tests/static-check.mjs` を更新し、次を追加しました。

- `workspace.css` 必須存在確認
- `workspace.css` が旧 `product-v2.css / library-tools-v22.css` より後に読み込まれること
- `app-config.js / project-meta.json / index.html` のVersion整合
- Guide Version / Schema Versionのmetadata整合
- v2.2ライブラリ機能CSS / JSが引き続き接続されていること

長期判断は新規 `PROJECT_LEARNINGS.md` に記録しました。
Visualの比較基準は `docs/VISUAL_BASELINE.md` にCandidateとして記録し、ユーザーの実画面確認前はUser Validated扱いにしていません。

### 変更ファイル

新規:

- `workspace.css`
- `PROJECT_LEARNINGS.md`
- `docs/VISUAL_BASELINE.md`

更新:

- `index.html`
- `app-config.js`
- `project-meta.json`
- `tests/static-check.mjs`
- `README.md`
- `作業報告書.md`

### 確認状態

- Implemented: **Yes**
- Storage / Schema change: **No**
- Static Validation: **Passed** — main merge commit `30b6b6d85c6cff08a4dc4ee4020299e29996f002` で共通Baseline / ASMRTube Static Check成功
- GitHub Pages deployment: **Passed** — 同じmain merge commitのPages build / deployment成功
- Visual Browser / Screenshot Review: **Not verified**
- Mobile real-device: **Not verified**

### 後続評価

2026-09-01のユーザーVisual Reviewで **40 / 100**。v2.4 Visual CandidateはRejectedとし、v3で基礎から再設計しています。

---

## 2026-08-29 v2.2 ライブラリ機能強化

### 今回変更した内容

#### 続きから再生

- 動画ごとに再生位置を自動保存
- `resumeAt / resumeDuration / resumeUpdatedAt` を作品オブジェクトの任意フィールドとして追加
- 再生中 / 一時停止中だけ保存
- YouTube Playerの `CUED / UNSTARTED` 状態では保存位置を書き換えない
- 8秒未満は保存しない
- 動画終了時、終了15秒前付近、97.5%以上再生時は続き位置をクリア
- 作品情報へ `前回の続き` と `▶ 続きから` を表示
- ページ非表示 / pagehideでも可能な範囲で最終位置を保存

#### お気に入り区間

- 作品ごとに名前付きA-B区間を保存可能にした
- 保存形式 `favoriteSections[]` を追加
- A-Bリピート設定済みなら開始 / 終了を自動入力
- A-B未設定なら現在位置から30秒を仮入力
- 保存済み区間を押すと、その区間をA-Bリピートで再生
- 区間削除を追加
- 区間名を左検索欄の検索対象へ追加
- 動画切替時に前動画のA-B設定を解除し、別動画へ誤って持ち越さないようにした

#### 配信者ページ

- 作品情報へ `配信者ページ` を追加
- 上部の配信者名からも開けるようにした
- 同じ配信者名の登録作品をまとめて表示
- 作品数 / お気に入り / 睡眠向け / 総タイムスタンプ数を表示
- よく使うタグを表示
- 各作品の評価と続き位置を表示
- 作品カードから直接そのASMRへ移動

#### 保存済みタイムスタンプ編集

- タイムスタンプパネルへ `編集` ボタンを追加
- 取り込み済みデータを再取り込みせず直接編集可能にした
- 編集対象:
  - 見出し `group`
  - 時刻
  - 内容 `label`
  - 副題 `subtitle`
- `＋ 現在位置` を追加
- `＋ 空の行` を追加
- 行削除を追加
- 編集途中で行追加 / 行削除しても、入力中の内容を保持する処理を追加
- 保存時に時刻順へ自動整列
- ラベル / 見出し / 副題変更後にタグ候補を再計算
- 親タイムスタンプの時刻・名前変更時、子の `parentTime / parentLabel` を追従
- 親を削除して参照先がなくなった子は通常タイムスタンプへ戻す

#### 自動品質チェック

新規追加:

- `.github/workflows/quality-check.yml`
- `tests/static-check.mjs`

mainへのpush / PRごとに以下を自動確認する。

- 全 `.js / .mjs` を `node --check`
- JSONファイルを構文解析
- `index.html` のローカルCSS / JS参照先が存在するか
- 必須ファイルが不足していないか
- v2.2 CSS / JSが `index.html` へ接続されているか

初回quality checkで以下を確認済み。

- JavaScript syntax: success
- Static project integrity: success

### 保存互換性

既存の保存キーは変更していない。

```text
asmrtube.library.v1
asmrtube.settings.v1
asmrtube.timestamp.view.v1
```

新規項目は作品オブジェクト内の任意フィールド。

```text
resumeAt
resumeDuration
resumeUpdatedAt
favoriteSections
```

旧バックアップJSONにこれらがなくても読み込み可能。
v2.1のJSON読み込み処理は元オブジェクトを展開して保持するため、新規任意フィールドもバックアップ / 復元対象になる。

### 修正した不具合 / 事故予防

- 動画を選択しただけの `CUED` 状態で保存済み続き位置が0秒扱いされる可能性を除去
- タイムスタンプ編集中に行追加 / 行削除した時、まだ保存していない入力が戻る可能性を除去
- A-B区間が別動画へ持ち越される問題をv2.2側で解除
- お気に入り区間の追加 / 削除後、作品情報UIを即時更新
- 今後JS構文エラーやファイル参照漏れをGitHub Actionsで検出可能にした

---

## 2026-08-29 v2.1 日常利用品質・安全性改善

- タイムスタンプ本文 / 見出し / 副題 / 親名まで検索対象を拡張
- 0件時の空状態と解除導線を追加
- YouTube IFrame API準備前の選択 / 再生予約
- cue済み作品を実際に再生した時も最近聴いたへ記録
- ASMR削除後8秒Undo
- 再生中作品削除時にPlayer / A-Bを初期化
- JSON読み込み前の検証、重複整理、壊れた参照除外、事前確認
- データ管理へライブラリ整合性診断を追加
- `role / parentTime / parentLabel / subtitle` を使った親子タイムスタンプ表示補強
- `Space / K / J / L` 再生ショートカット
- Toast / 検索 / シーク / 音量のアクセシビリティ改善

---

## 2026-08-29 v2.0 Product Shell

- Dashboard
- データ管理画面
- ローカルスナップショット
- タイムスタンプ解析Confidence表示
- モバイルドロワー
- 表示設定
- ヘルプ

---

## v1.9以前の主要履歴

### v1.9 汎用タイムスタンプ解析

- 普通の `0:00 内容`
- Markdownリンク
- ハイフン区切り
- 括弧付き時刻
- 改行が潰れた見出し形式
- `▷ / ┗` 親子
- `└` 混在
- YouTubeカスタム絵文字
- 日本語 + 英語
- URL `t / start`
- 2時間超え
- 任意 `role / parentTime / parentLabel / subtitle / confidence / sourceStyle`
- 回帰テスト10系統

### v1.8

- タイムスタンプ `見出し / すべて`
- 親カード
- 折りたたみ
- 再生中グループ強調

### v1〜v1.7

- ASMRライブラリ
- YouTube再生
- 検索 / タグ / 評価 / お気に入り / 履歴 / 睡眠向け
- プレイリスト
- コメントからタイムスタンプ抽出
- A-Bリピート
- スリープタイマー
- JSONバックアップ / 復元
